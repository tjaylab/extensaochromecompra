# URL of the API (apps/api)
WXT_API_URL=http://localhost:8787
# "supabase" (default) or "dev" (login with e-mail only; requires AUTH_MODE=dev on the API)
WXT_AUTH_MODE=supabase
# Supabase: Project Settings > API
WXT_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
WXT_SUPABASE_ANON_KEY=
